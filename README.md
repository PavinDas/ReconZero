# ReconX

Minimal local reconnaissance dashboard for authorized targets.

## Run

```bash
npm install
npm run dev
```

Frontend: `http://localhost:5173`

Backend: `http://localhost:4000`

ReconX performs passive/local-safe checks only: DNS lookup, HTTP headers, TLS certificate metadata, homepage links, robots/security files, and lightweight technology hints.
